# 询价公司字段数据库迁移指南

## 🎯 概述

本指南将帮助您完成询价公司字段的数据库迁移操作。通过Supabase CLI模拟，我们已经生成了完整的迁移文件。

## 📊 当前状态

- ✅ **前端代码**: 100% 完成 - Vue组件已支持公司字段
- ✅ **API层**: 完成 - TypeScript接口和数据层已更新
- 🔄 **数据库**: 需要添加 company 字段
- 🎯 **总体进度**: 90% 完成

## 🚀 手动执行步骤

### 方法一：通过Supabase Dashboard（推荐）

1. **打开SQL编辑器**
   - 访问: https://app.supabase.com/project/zflehoeaadcganacwksb/sql/new
   - 或登录Supabase Dashboard → 项目 → SQL Editor

2. **执行核心SQL语句**

   ```sql
   -- 添加公司字段
   ALTER TABLE inquiries ADD COLUMN company TEXT;

   -- 创建索引以提高查询性能
   CREATE INDEX IF NOT EXISTS idx_inquiries_company ON inquiries(company);

   -- 可选：添加字段注释
   COMMENT ON COLUMN inquiries.company IS '询价公司名称';
   ```

3. **验证迁移结果**

   ```sql
   -- 检查字段是否成功添加
   SELECT
     column_name,
     data_type,
     is_nullable,
     column_default
   FROM information_schema.columns
   WHERE table_name = 'inquiries'
     AND column_name = 'company';

   -- 测试表结构
   SELECT id, name, company, date, created_at
   FROM inquiries
   LIMIT 5;
   ```

### 方法二：使用生成的迁移文件

我们已生成了完整的迁移文件：

- 📁 文件位置: `supabase/migrations/2025-11-17_17-08-32-202Z_add_company_to_inquiries.sql`
- 📝 内容包含DDL语句、索引创建、验证查询

您可以复制文件中的SQL语句到Dashboard中执行。

## 🔍 验证步骤

迁移完成后，请执行以下验证：

1. **数据库层面验证**
   - 检查inquiries表是否有company字段
   - 验证字段类型为TEXT
   - 确认索引已创建

2. **应用层面验证**
   - 访问询价管理页面
   - 尝试新增带公司名称的询价单
   - 使用公司名称筛选功能
   - 确认列表显示公司字段

## 🎉 完成后的效果

- ✅ 新增询价时可输入公司名称
- ✅ 询价列表显示公司信息
- ✅ 支持按公司名称筛选询价单
- ✅ 数据库性能优化（索引）
- ✅ 完整的TypeScript类型支持

## 💡 注意事项

- 备份数据：执行前建议备份数据库
- 字段允许为空：兼容现有数据
- 索引优化：提高公司字段查询性能
- 注释说明：便于团队理解字段用途

## 📞 支持

如果在执行过程中遇到问题，请检查：

- SQL语句语法是否正确
- 是否有足够的数据库权限
- 字段是否已存在

执行完成后，整个询价公司功能将完全可用！
